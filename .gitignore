# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code local permission overrides (per-developer, not shared)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Convex codegen output — regenerated by `npx convex dev` / `convex deploy` /
# `npx convex codegen`. `_generated/ai/` holds static installed guideline
# docs, not codegen output, so it's kept.
/convex/_generated/*
!/convex/_generated/ai/
/convex/betterAuth/_generated/

# TanStack Router codegen output — regenerated by the router vite plugin on
# every `vite build` / `vite dev`.
/src/routeTree.gen.ts
